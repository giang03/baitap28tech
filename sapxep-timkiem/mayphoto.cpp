#include <bits/stdc++.h>

using namespace std;
using ll = long long;
const int MOD = 1e9 + 7;

int main() {
	int n,x,y; cin >> n >> x >> y;
    int a = min(x,y), b = max(x,y);
    int dem = a;
    n -= 1;
    for(int i = 0; i < n; i++){
        
    }
    return 0;
}

